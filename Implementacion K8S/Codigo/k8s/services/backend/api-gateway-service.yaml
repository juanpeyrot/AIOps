apiVersion: v1
kind: Service
metadata:
  name: pharmago-api-gateway
  namespace: pharmago
spec:
  selector:
    app: pharmago-api-gateway
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP

