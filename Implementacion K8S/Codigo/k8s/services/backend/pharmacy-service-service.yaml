apiVersion: v1
kind: Service
metadata:
  name: pharmago-pharmacy-service
  namespace: pharmago
spec:
  selector:
    app: pharmago-pharmacy-service
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP

